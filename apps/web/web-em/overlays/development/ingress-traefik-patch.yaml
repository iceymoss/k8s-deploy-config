apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-ingress
spec:
  rules:
    - host: dev.web.test.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web-em
                port:
                  number: 80